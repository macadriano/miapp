# Configuración para PRODUCCIÓN
# Cambiar estos valores por los de tu servidor

# Django
DEBUG=False
SECRET_KEY=tu-clave-secreta-super-segura-cambiar-en-produccion
ALLOWED_HOSTS=tu-servidor.com,IP-del-servidor,localhost,127.0.0.1

# Base de Datos PostgreSQL
DB_NAME=waygps
DB_USER=tu_usuario_db
DB_PASSWORD=tu_password_db
DB_HOST=localhost
DB_PORT=5432

# Seguridad (solo si usas HTTPS)
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# Redis (para WebSockets - opcional)
REDIS_URL=redis://localhost:6379/0

# Email (opcional)
EMAIL_HOST=smtp.tu-proveedor.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-email@dominio.com
EMAIL_HOST_PASSWORD=tu-password-email

# Configuración Sofia
# Setear a True si el servidor tiene poca memoria (<1GB) para evitar errores 500
SOFIA_LITE_MODE=True
